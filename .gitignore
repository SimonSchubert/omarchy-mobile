# Build outputs. The image is 5-12 GB and is rebuilt from the manifest, so it
# is not something git should ever have seen.
vm/out/

# The pacman download cache lives in a Docker named volume
# (omarchy-mobile-pkgcache), not here -- see the note in scripts/vm-build.sh.
.cache/

.DS_Store
